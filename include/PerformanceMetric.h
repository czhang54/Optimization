
enum PerformanceMetric
{
	AVERAGE_VALUE, // Average function value of the particles at each iteration
	BEST_VALUE_FOUND, // The best (minimum) value found at each iteration
	DISTANCE_ERROR // Distance between the particle mean and the global minimizer
};